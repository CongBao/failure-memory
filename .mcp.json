{
  "mcpServers": {
    "failure-memory": {
      "command": "failure-memory",
      "args": [
        "mcp",
        "--stdio"
      ],
      "env": {
        "FAILURE_MEMORY_HARNESS": "plugin"
      },
      "startup_timeout_sec": 5,
      "tool_timeout_sec": 10
    }
  }
}
