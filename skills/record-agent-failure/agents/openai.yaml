interface:
  display_name: "Record Agent Failure"
  short_description: "Qualify and record durable agent failures"
  default_prompt: "Use $record-agent-failure to qualify this correction and record it only if it is a real durable failure."
